<%= form_with scope: :person, url: persons_search_path, method: 'get', local: true do |form| %>
  <p>
    <%= form.label :bp %><br>
    <%= form.text_field :bp %>
  </p>

  <p>
    <%= form.submit :Buscar %>
  </p>
<% end %>
<%= link_to 'Nuevo ingreso', new_person_path %>